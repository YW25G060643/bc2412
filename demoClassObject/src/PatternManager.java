import javax.smartcardio.Card;

public class PatternManager {
    private Card[] cards.length != 5;

    public PatternManager(Card[] cards) {
        if (cards == null || cards.length != 5) {
          
        }
    }
}
